---
apiVersion: source.toolkit.fluxcd.io/v1beta2
kind: OCIRepository
metadata:
  name: dashboards
  namespace: giantswarm
spec:
  interval: 10m
  provider: generic
  ref:
    semver: x.x.x
  url: oci://gsoci.azurecr.io/charts/giantswarm/dashboards
---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: dashboards
  namespace: giantswarm
spec:
  chartRef:
    kind: OCIRepository
    name: dashboards
    namespace: giantswarm
  install:
    remediation:
      remediateLastFailure: false
      retries: 10
  interval: 5m
  releaseName: dashboards
  storageNamespace: monitoring
  targetNamespace: monitoring
  timeout: 10m
  upgrade:
    remediation:
      remediateLastFailure: true
      retries: 10
      strategy: rollback
  valuesFrom:
  - kind: ConfigMap
    name: dashboards-konfiguration
    valuesKey: configmap-values.yaml
  - kind: Secret
    name: dashboards-konfiguration
    valuesKey: secret-values.yaml
