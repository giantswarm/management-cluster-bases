---
apiVersion: source.toolkit.fluxcd.io/v1beta2
kind: OCIRepository
metadata:
  name: event-exporter-app
  namespace: giantswarm
spec:
  interval: 10m
  provider: generic
  ref:
    semver: x.x.x
  url: oci://gsoci.azurecr.io/charts/giantswarm/event-exporter-app
---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: event-exporter-app
  namespace: giantswarm
spec:
  chartRef:
    kind: OCIRepository
    name: event-exporter-app
    namespace: giantswarm
  install:
    remediation:
      remediateLastFailure: false
      retries: 10
  interval: 5m
  releaseName: event-exporter-app
  storageNamespace: giantswarm
  targetNamespace: giantswarm
  timeout: 10m
  upgrade:
    remediation:
      remediateLastFailure: true
      retries: 10
      strategy: rollback
  valuesFrom:
  - kind: ConfigMap
    name: event-exporter-app-konfiguration
    valuesKey: configmap-values.yaml
  - kind: Secret
    name: event-exporter-app-konfiguration
    valuesKey: secret-values.yaml
