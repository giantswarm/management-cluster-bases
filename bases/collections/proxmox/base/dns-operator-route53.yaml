---
apiVersion: source.toolkit.fluxcd.io/v1beta2
kind: OCIRepository
metadata:
  name: dns-operator-route53
  namespace: giantswarm
spec:
  interval: 10m
  provider: generic
  ref:
    semver: x.x.x
  url: oci://gsoci.azurecr.io/charts/giantswarm/dns-operator-route53
---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: dns-operator-route53
  namespace: giantswarm
spec:
  chartRef:
    kind: OCIRepository
    name: dns-operator-route53
    namespace: giantswarm
  install:
    remediation:
      remediateLastFailure: false
      retries: 10
  interval: 5m
  releaseName: dns-operator-route53
  storageNamespace: giantswarm
  targetNamespace: giantswarm
  timeout: 10m
  upgrade:
    remediation:
      remediateLastFailure: true
      retries: 10
      strategy: rollback
  valuesFrom:
  - kind: ConfigMap
    name: dns-operator-route53-konfiguration
    valuesKey: configmap-values.yaml
  - kind: Secret
    name: dns-operator-route53-konfiguration
    valuesKey: secret-values.yaml
