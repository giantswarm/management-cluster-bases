---
apiVersion: source.toolkit.fluxcd.io/v1beta2
kind: OCIRepository
metadata:
  name: cluster-api-provider-proxmox
  namespace: giantswarm
spec:
  interval: 10m
  provider: generic
  ref:
    semver: x.x.x
  url: oci://gsoci.azurecr.io/charts/giantswarm/cluster-api-provider-proxmox
---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: cluster-api-provider-proxmox
  namespace: giantswarm
spec:
  chartRef:
    kind: OCIRepository
    name: cluster-api-provider-proxmox
    namespace: giantswarm
  install:
    remediation:
      remediateLastFailure: false
      retries: 10
  interval: 5m
  releaseName: cluster-api-provider-proxmox
  storageNamespace: giantswarm
  targetNamespace: giantswarm
  timeout: 10m
  upgrade:
    remediation:
      remediateLastFailure: true
      retries: 10
      strategy: rollback
  valuesFrom:
  - kind: ConfigMap
    name: cluster-api-provider-proxmox-konfiguration
    valuesKey: configmap-values.yaml
  - kind: Secret
    name: cluster-api-provider-proxmox-konfiguration
    valuesKey: secret-values.yaml
