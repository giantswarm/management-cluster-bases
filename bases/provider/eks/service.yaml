apiVersion: v1
kind: Service
metadata:
  name: helloworld-eks
  labels:
    app: helloworld-eks
  namespace: default
spec:
  type: ClusterIP
  ports:
  - port: 8080
  selector:
    app: helloworld-eks
