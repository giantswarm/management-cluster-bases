apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: mcp-prometheus
  namespace: flux-giantswarm
spec:
  releaseName: mcp-prometheus
  chartRef:
    kind: OCIRepository
    name: mcp-prometheus
    namespace: flux-giantswarm
  install:
    remediation:
      retries: 10
      remediateLastFailure: false
  interval: 10m
  targetNamespace: mcp-prometheus
  timeout: 10m
  upgrade:
    remediation:
      retries: 10
      remediateLastFailure: false
  valuesFrom:
    # ConfigMap rendered by Konfiguration from shared-configs template
    - kind: ConfigMap
      name: mcp-prometheus-konfiguration
      valuesKey: configmap-values.yaml
